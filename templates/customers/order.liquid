{%- comment -%} SCAFFOLD — full order detail is built in Task 03. {%- endcomment -%}
<section class="w-full" style="background-color: var(--color-brand-beige);">
  <div class="container mx-auto px-4 py-10 md:!py-16">
    <h1 class="h1 text-espresso mb-2">{{ order.name }}</h1>
    <p class="body text-espresso">
      {{ order.created_at | date: '%B %d, %Y' }} · {{ order.financial_status_label }} · {{ order.fulfillment_status_label }}
    </p>
  </div>
</section>
