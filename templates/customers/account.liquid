{%- comment -%} SCAFFOLD — full dashboard states are built in Task 02. {%- endcomment -%}
<section class="w-full" style="background-color: var(--color-brand-beige);">
  <div class="container mx-auto px-4 py-10 md:!py-16">
    <div class="flex items-center justify-between mb-6">
      <h1 class="font-kurdis-condensed font-extrabold uppercase text-espresso text-[32px] md:!text-[45px] leading-none">
        {{ 'customer.account.welcome' | t: first_name: customer.first_name }}
      </h1>
      <a href="{{ routes.account_logout_url }}" class="tag text-espresso hover:underline">{{ 'customer.account.log_out' | t }}</a>
    </div>
    <p class="body text-espresso">Account dashboard — coming soon.</p>
  </div>
</section>
