{%- comment -%}
  PREVIEW-ONLY helper template (remove before merge).
  Renders the account dashboard section on a public /pages/ URL so it can be
  reviewed without logging in. Shows the FREE state with mock data
  (no customer context). Real tier/credits/orders appear only on /account when
  logged in.

  How to use: Admin → Online Store → Pages → Add page → Theme template:
  "account-preview" → Save → open /pages/<your-handle>.
{%- endcomment -%}
{% section 'customer-account-dashboard' %}
